using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CoinsSpawner : MonoBehaviour
{
    // public GameObject objectToSpawn; // префаб объекта, который нужно спавнить
    // public int numberOfObjects; // количество объектов для спавна
    // public float spawnRadius; // радиус, в котором должны спавниться объекты

    // void Start()
    // {
    //     for (int i = 0; i < numberOfObjects; i++)
    //     {
    //         Vector3 randomPosition = transform.position + Random.insideUnitSphere * spawnRadius; // генерация случайной позиции в радиусе
    //         Instantiate(objectToSpawn, randomPosition, Quaternion.identity); // создание объекта на случайной позиции
    //     }
    // }
}
